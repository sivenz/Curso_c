#include <stdio.h>
#include <stdlib.h>

int main(){
    
    int a = 10, b = 20;

    /*
    float pi = 3.14155;

    a = (int)pi;
    */

    // % na divisão de c serve para retornar o resto de uma divisão inteira


    printf("resto da divisão inteira: %d\n", b % a);

    return 0;
}
