#include <stdio.h>
#include <stdlib.h>

int main(){

    float x = 1.0;

    printf("Tamanho em memoria de um float: %d bytes\n", sizeof(x));
    printf("Tamanho em memoria de int: %d bytes\n", sizeof(int));

    // 1 bytes = 8 bit

    return 0;
}