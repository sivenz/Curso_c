#include <stdio.h>
#include <stdlib.h>

int main(){
    
    printf("isto eh uma mensagem a ser impressa na tela.");
    // \n = saltar linha
    // \t = como se fosse um tab antes do texto
    // \' = pra usar '' no texto
    // \" = pra usar "" no texto
    // \\ = pra usar \ no texto

    return 0;
}
